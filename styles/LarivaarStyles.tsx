import { StyleSheet } from 'react-native';
import font from '@utils/font';

export const LarivaarStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    padding: 15,
  },
  fontSizeText: {
    fontSize: 20,
    fontFamily: font.Baloo_Paaji_2_Regular,
    color: '#11336A',
  },
});
